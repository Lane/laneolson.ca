# Lane Olson Portfolio Website

## Installation

Clone this repo and run the following command in the base directory:

`sudo npm install`

then

`bower install`

After the packages are installed, run `gulp` 